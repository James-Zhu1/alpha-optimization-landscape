.venv/
__pycache__/
.pytest_cache/
.cache/
*.py[cod]
.DS_Store
.Rhistory
data/raw/*.parquet
data/processed/*.parquet
.timing_*.parquet

# Downloaded panels stay out of version control; they are large and rebuildable
# from config.yaml. The forward-test cache is regenerated on every refresh.
data/forward_raw/
data/forward_processed/

# Figures and results/ are portfolio artifacts and ARE committed. They let a
# reader verify every number in REPORT.md, and let the dashboard run on a fresh
# clone, without downloading data or rerunning the 700-evaluation search.

# Local editor/agent tooling, not part of the project.
.claude/
